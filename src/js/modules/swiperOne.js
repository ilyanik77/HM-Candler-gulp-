import Swiper from 'swiper/bundle';
import 'swiper/css/bundle';

const swiperOne = new Swiper('.feedback__slider', {

	loop: true,

	// If we need pagination
	pagination: {
		el: '.swiper-pagination',
	},
})

export default swiperOne = swiperOne;
